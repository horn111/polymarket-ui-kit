{
  "name": "@polymarket-ui-kit/cli",
  "version": "0.0.0",
  "description": "CLI helpers for installing and validating Polymarket UI Kit.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "polymarket-ui-kit": "./dist/index.js",
    "pui": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx src/index.ts doctor"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
