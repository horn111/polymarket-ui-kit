# Embed Card Example

Use `ShareCard` for editorial embeds, partner pages, and social screenshots.

```tsx
import { ShareCard } from "@polymarket-ui-kit/react";

export function Embed({ market }) {
  return <ShareCard market={market} attribution="your-site.com" />;
}
```

