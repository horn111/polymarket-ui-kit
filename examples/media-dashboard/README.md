# Media Dashboard Example

Suggested layout for newsrooms and research portals:

- `MarketCard` for the lead market.
- `ProbabilityChart` for market movement.
- `CommentList` for social context.
- `ShareCard` for X-ready exports.

